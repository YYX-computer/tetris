# tetris
tetris

to install dependencies by:
```shell
pip install tkinter
pip install pygame
pip install random
pip install time
```
*What?You didn't have Pip?*

and to run it:
```shell
python main.py
```
*What?You didn't have Python?*
